/// @description Name the button properly
event_inherited();
button_text = "Sounds on";

// Disable button until it makes sense to have it
is_selectable = true;

// Start with music on
is_pressed = true;